from model import get_model
import torch

# 우리가 사용하는 입력 특성 개수는 7개입니다 (close, volume, ma5, ma20, rsi, macd, bollinger)
model = get_model(input_size=7)

# 'best_model.pt'라는 이름으로 저장
torch.save(model.state_dict(), "best_model.pt")

print("✅ 모델 저장 완료!")
